# action.yml
name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  path:
    required: true
    description: ''
    default: 'World'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: ${{ inputs.path +'/Dockerfile' }}